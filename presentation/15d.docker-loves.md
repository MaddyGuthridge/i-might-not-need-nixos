# What I like about Docker

* It is used everywhere.
* There is probably already a good image to use as a base, and so many examples
  to use as a starting point.
* Once things are dockerized, running them is so easy with `docker compose`.
* This is *excellent* for servers and home-labs.
