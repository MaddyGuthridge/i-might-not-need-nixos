# What I dislike about Mise

If the tool you want to install has complex dependencies that aren't handled by 
existing integrations, you'll find yourself writing Lua to create installation 
scripts that set up these dependencies manually, which is far worse than what 
it's like with Nix.
